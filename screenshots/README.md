# Screenshots cần chụp khi nộp

Lưu đúng các tên file sau:

- `prefect_ui.png`: Prefect UI tại http://localhost:4200, flow/deployment đang chạy.
- `api_gateway.png`: kết quả `curl http://localhost:8000/health`.
- `grafana_dashboard.png`: Grafana tại http://localhost:3000.

Hai ảnh ở root repo:

- `smoke_tests_results.png`: kết quả `pytest smoke-tests/ -v`.
- `production_readiness.png`: kết quả `python scripts/production_readiness_check.py`.
